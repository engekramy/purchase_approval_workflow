<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================
         Purchase > Approvals  (top-level section for approvers)
    ============================================================ -->
    <menuitem
        id="menu_my_pending_approvals"
        name="My Pending Approvals"
        parent="purchase.menu_procurement_management"
        action="purchase_approval_workflow.action_my_pending_approvals"
        sequence="10"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase_approval_workflow.group_purchase_approval_administrator"/>

    <menuitem
        id="menu_all_pending_approvals"
        name="All Pending Approvals"
        parent="purchase.menu_procurement_management"
        action="purchase_approval_workflow.action_all_pending_approvals"
        sequence="11"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase_approval_workflow.group_purchase_approval_administrator"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow  (parent category)
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_workflow_category"
        name="Approvals Workflow"
        parent="purchase.menu_purchase_config"
        sequence="20"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Approval Matrix
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_matrix"
        name="Approval Matrix"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_matrix"
        sequence="10"
        groups="purchase_approval_workflow.group_purchase_approval_administrator,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Workflow Diagram
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_matrix_diagram"
        name="Workflow Diagram"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_matrix_diagram"
        sequence="20"
        groups="purchase_approval_workflow.group_purchase_approval_administrator,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Approval History
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_history"
        name="Approval History"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_history"
        sequence="30"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Approval Analytics  (Feature 9)
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_analytics"
        name="Approval Analytics"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_analytics"
        sequence="40"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Apply Policy Template  (Feature 11)
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_policy_wizard"
        name="Apply Policy Template"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_policy_wizard"
        sequence="50"
        groups="purchase_approval_workflow.group_purchase_approval_administrator,purchase.group_purchase_manager"/>

    <!-- ============================================================
         Purchase > Configuration > Approvals Workflow > Export Audit Trail  (Feature 14)
    ============================================================ -->
    <menuitem
        id="menu_purchase_approval_audit_export"
        name="Export Audit Trail"
        parent="purchase_approval_workflow.menu_purchase_approval_workflow_category"
        action="purchase_approval_workflow.action_purchase_approval_audit_export_wizard"
        sequence="60"
        groups="purchase_approval_workflow.group_purchase_approval_manager,purchase.group_purchase_manager"/>

</odoo>
